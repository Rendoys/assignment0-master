# Buat Laporan di Sini
Nama: Rendy Ahmad Nafis
NRP: 5024251043

# 1. Hasil Jalannya Program
Program telah berhasil dimodifikasi untuk menerima input nama dari user dan menampilkan sapaan "Hello". Berikut adalah cuplikan saat program dijalankan di terminal WSL:

- Perintah Build: `make clean` lalu `make`.
- Perintah Run: `./MyApp`.
- Input: `Buni Ani`.
- Output: `Hello Buni Ani`.

# 2. Perubahan Nama Output
Sesuai dengan tugas tambahan, nama file output telah diubah dari `myapp` menjadi `MyApp`. Perubahan ini dilakukan pada file `Makefile` di baris `APPNAME = MyApp`.

# 3. Struktur Folder
Folder tugas ini terdiri dari:
- `src/main.cpp`: Kode sumber utama C++.
- `Makefile`: Konfigurasi untuk melakukan compile.
- `MyApp`: File executable hasil compile.
- `REPORT.md`: Laporan pengerjaan tugas ini.